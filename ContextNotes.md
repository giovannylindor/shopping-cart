# Context Notes

* Context: A mechanism for react to pass state for _ANY_ component to read
* Provider: A component that broadcasts that state onto the components nested inside it
    - Any component inside the provider can pull the value directly